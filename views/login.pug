extends layout

block content
  .form-container
    h2 Iniciar Sesión 
    p.subtitle ¡Qué bueno verte de nuevo en Fotaza!
    
    form(action="/api/usuarios/login" method="POST")
      .form-group
        label(for="email") Correo Electrónico
        input#email(type="email" name="email" placeholder="tu-email@correo.com" required)
      
      .form-group
        label(for="password") Contraseña
        input#password(type="password" name="password" placeholder="Ingresá tu contraseña" required)
      
      button.btn-submit(type="submit") Ingresar
    
    p.form-footer ¿No tenés cuenta todavía? 
      a(href="/registro") Registrate acá